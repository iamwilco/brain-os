/**
 * Vault module
 * Handles vault operations and initialization
 */

export { initVault, VAULT_DIRECTORIES, getTemplateFiles } from './init.js';
export type { InitOptions, InitResult, TemplateFile } from './init.js';
